# "mock"   -> deterministic offline function-calling router (no key)
# "gemini" -> Google Gemini native function calling (requires key)
LLM_PROVIDER=mock
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

DB_PATH=data/store.db
MAX_TOOL_STEPS=4
